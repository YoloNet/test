# Security Audit`n`n`OTHERS/rclone.conf` contains an exposed Google OAuth token. `BACKUP/backupgithub.sh` contains an exposed GitHub personal access token. `setup-lite.sh` stores Telegram bot token/chat ID in plaintext under `/root/yoloautosc`. Values are redacted here; revoke and rotate them.`n`nInstallers execute remote scripts and binaries without checksum/signature verification; `SETUP/set-br.sh` pipes a remote installer to Bash. Restore scripts download/extract archives into privileged paths. User input is interpolated into `sed`, paths, URLs, JSON, service units, and shell commands. JSON edits are non-atomic. The Xray private key is chmod 755. Root services, plaintext backups, Telegram transfers, log/history clearing, duplicate cron jobs, and weak error handling increase risk.
